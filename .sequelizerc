/**
 * Como este arquivo não tem a extensão JS, precisamos pressionar CTRL+SHIFT+P e selecionar
 * para a linguagem Javascript para que a gestão de sintaxe seja aplicada.
 *
 * Outro informação importante é que neste arquivo usaremos o CommonJS
 */

const path = require('path');

module.exports = {
  //caminho para arquivo de configuração da base de dados
  config: path.resolve(__dirname, 'src', 'config', 'database.js'),

  //caminho para models
  'models-path': path.resolve(__dirname, 'src', 'models'),

  //caminho para as migrations - esa pasta não precisa ser criada
  'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),

  //caminho para os seeders
  'seeders-path': path.resolve(__dirname, 'src', 'database', 'seeds'),
};
